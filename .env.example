# Strapi Cloud Configuration
# Get these values from https://strapi.io/cloud after creating your project

# Strapi Cloud API URL (replace with your project URL)
VITE_STRAPI_URL=https://your-project-name.strapi.cloud

# Strapi API Token (create a Read-Only token in Strapi Settings → API Tokens)
VITE_STRAPI_TOKEN=your_api_token_here

# Note: After creating .env.local with real values, never commit it to git
# The .env.local file is already in .gitignore
