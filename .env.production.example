# Frontend
FRONTEND_PORT=80
VITE_STRAPI_TOKEN=your_api_token_from_strapi_admin

# Strapi admin auto-creation (optional, remove after first start)
STRAPI_ADMIN_EMAIL=admin@iro.by
STRAPI_ADMIN_PASSWORD=ChangeMe123!
STRAPI_ADMIN_FIRSTNAME=Admin
STRAPI_ADMIN_LASTNAME=IRO

# Strapi secrets (generate with: openssl rand -base64 16)
APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=random_salt_here
ADMIN_JWT_SECRET=random_secret_here
TRANSFER_TOKEN_SALT=random_salt_here
JWT_SECRET=random_secret_here

# Cloudflare Tunnel (optional — for internet access without domain)
# Leave empty for quick tunnel (random URL), or set token for named tunnel
CLOUDFLARE_TUNNEL_TOKEN=
